<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="NotificationStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusM}"/>
        <Setter Property="Padding" Value="{StaticResource PaddingL}"/>
        <Setter Property="Margin" Value="{StaticResource MarginM}"/>
        <Setter Property="Effect" Value="{StaticResource ShadowL}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <Style x:Key="SuccessNotificationStyle" TargetType="Border" BasedOn="{StaticResource NotificationStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessThin}"/>
    </Style>

    <Style x:Key="WarningNotificationStyle" TargetType="Border" BasedOn="{StaticResource NotificationStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessThin}"/>
    </Style>

    <Style x:Key="ErrorNotificationStyle" TargetType="Border" BasedOn="{StaticResource NotificationStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource ErrorBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessThin}"/>
    </Style>

    <Style x:Key="InfoNotificationStyle" TargetType="Border" BasedOn="{StaticResource NotificationStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessThin}"/>
    </Style>

    <Style x:Key="ToastStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource OnSurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusM}"/>
        <Setter Property="Padding" Value="{StaticResource PaddingM}"/>
        <Setter Property="Margin" Value="{StaticResource MarginM}"/>
        <Setter Property="Effect" Value="{StaticResource ShadowM}"/>
    </Style>

    <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Background" Value="{DynamicResource SurfaceVariantBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Height" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{StaticResource CornerRadiusXS}"/>
                        <Rectangle x:Name="PART_Indicator"
                                   Fill="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Left"
                                   RadiusX="{StaticResource CornerRadiusXS}"
                                   RadiusY="{StaticResource CornerRadiusXS}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
